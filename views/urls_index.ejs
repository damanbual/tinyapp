
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My URLs</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css">
</head>
<body>
  <%- include('partials/_header') %>
  <main style="margin: 1em;">
    <h3>My URLs</h3>
    <% if (!user) { %>
      <p>Please <a href="/login">log in</a> or <a href="/register">register</a> to view your URLs.</p>
    <% } else { %>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Short URL</th>
            <th scope="col">Long URL</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
          </tr>
        </thead>
        <tbody>
          <% for (let id in urls) { %>
            <tr>
              <td><%= id %></td>
              <td><a href="<%= urls[id].longURL %>"><%= urls[id].longURL %></a></td>
              <td><a href="/urls/<%= id %>">Edit</a></td>
              <td>
                <form method="POST" action="/urls/<%= id %>/delete">
                  <button type="submit" class="btn btn-danger">Delete</button>
                </form>
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
      <a href="/urls/new" class="btn btn-primary">Create New URL</a>
    <% } %>
  </main>
</body>
</html>